<Window x:Class="FileViewerApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="File Viewer" Height="450" Width="600">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBox x:Name="FilePathTextBox" Grid.Row="0" Margin="10" Width="400" 
                 VerticalAlignment="Center" HorizontalAlignment="Left" 
                 TextWrapping="Wrap" AcceptsReturn="True" 
                 Text="Enter file path here..." TextChanged="FilePathTextBox_TextChanged"/>

        <Button x:Name="LoadFileButton" Grid.Row="0" Margin="10,0" 
                VerticalAlignment="Center" HorizontalAlignment="Right" 
                Width="100" Content="Load File" Click="LoadFileButton_Click"/>

        <TextBox x:Name="FileContentTextBox" Grid.Row="1" Margin="10" 
                 VerticalAlignment="Stretch" HorizontalAlignment="Center"
                 Width="500"
                 TextWrapping="Wrap" AcceptsReturn="True" 
                 Text="File content..." TextAlignment="Center" 
                 IsReadOnly="True" VerticalScrollBarVisibility="Auto"/>
    </Grid>
</Window>
